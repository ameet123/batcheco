package com.att.datalake.loco.offercriteria.model;
/**
 * at each step we capture the tables, step, columns data
 * in a convenience model class that way we don't have to 
 * deal with right/left columns/tables, aliases
 * we can encapsulate them here, thus making the
 * {@link PreProcProcessorData} class a little cleaner
 * @author ac2211
 *
 */
public class PreProcTabularData {

	
}
